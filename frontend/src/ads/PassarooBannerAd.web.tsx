// Web stub — ads never render on web.
import React from "react";
import type { ComponentType } from "react";
export const PassarooBannerAd: ComponentType<any> = () => null as unknown as React.ReactElement;
